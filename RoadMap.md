# Turn a single variable into array with smooth transition
# Display values of array through multiple hardcoded statement to pave the way to loops
# Demonstrate simple loop e.g. display from 0 to 10
# Apply for loop to array now, with details and smooth transition
# Now, take this code into a separate function
# let student understand parameters and arguments
# Now, adjust the deduction formula in this function
